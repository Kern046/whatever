{%  extends 'base.html.twig' %}

{% block body %}
    <nav>
        {% if is_granted('ROLE_USER') %}

        {% else %}
            <a href="{{ path('app_login') }}">
                {{ 'app.security.login.link' }}
            </a>
            <a href="{{ path('app_register') }}">
                {{ 'app.security.register.link' }}
            </a>
        {% endif %}
    </nav>

    <h1>Warhammer</h1>
{% endblock body %}